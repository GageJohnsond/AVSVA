# GUI and Visualization
PyQt5>=5.15.0
matplotlib>=3.5.0
seaborn>=0.11.0

# Data Processing
pandas>=1.3.0
numpy>=1.21.0
scipy>=1.7.0

# ROS Dependencies (ROS 1 Noetic)
# Note: rosbag and rospy are installed via apt with ROS:
#   sudo apt-get install ros-noetic-rosbag ros-noetic-rospy
# The following are pip-installable ROS utilities:
rospkg>=1.3.0
pyyaml>=5.4.0
catkin_pkg>=0.4.0
python-dateutil>=2.8.0
defusedxml>=0.7.0

# Alternative ROS bag reader (works without full ROS installation)
bagpy>=0.5
